@model IEnumerable<LaptopExportViewModel>
@{
    ViewData["Title"] = "Laptops";
}
<h1 class="text-center mb-2">@ViewData["Title"]</h1>
<hr />

<table class="table table-striped table-hover mt-5">
    <thead>
    <tr class="table-primary">
        <td>Brand</td>
        <td>CPU</td>
        <td>RAM</td>
        <td>SSD</td>
        <td>Video card</td>
        <td>Price</td>
        <td>Display size</td>
        <td>Warranty</td>
        <td>View</td>
    </tr>
    </thead>
    <tbody>
    @foreach (var laptop in Model)
    {
        <tr>
            <td>@laptop.Brand</td>
            <td>@laptop.CPU</td>
            <td>@laptop.RAM GB</td>
            <td>@laptop.SSDCapacity GB</td>
            <td>@laptop.VideoCard</td>
            <td>@laptop.Price EUR</td>
            <td>@laptop.DisplaySize Inches</td>
            <td>@laptop.Warranty Months</td>
            <td>
                    <a asp-controller="Laptop" asp-action="Details" asp-route-id="@laptop.Id" class="btn btn-warning">Details</a>
            </td>
        </tr>
    }
    </tbody>
</table>